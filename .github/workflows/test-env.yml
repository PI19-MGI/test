name: test env

on:
  workflow_dispatch:

env:
  CONFIG_ARGS: "bouh"
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: | 
          echo "$CONFIG_ARGS"
          echo "CONFIG_ARGS=baaa" >> $GITHUB_ENV
      - name: print
        run: | 
          echo "$CONFIG_ARGS"
          
